#Hugo
#Stretch & Challenge

def get_information():
    choice = input("Are you converting from imperial to metric or metric to imperial (M-I/I-M): ")
    if choice == "I-M":
        feet = float(input("How many feet: "))
        inches = float(input("How many inches: "))
        

def conversion():
    
